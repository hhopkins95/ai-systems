import { RuntimeConfig } from "../../types";
import { ExecutionEnvironment } from "./base";


export const getExecutionEnvironment = (sessionId : string, environment : RuntimeConfig["executionEnvironment"]) : ExecutionEnvironment => { 
}